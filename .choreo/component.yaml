# +required The configuration file schema version
schemaVersion: 1.1

# +optional Incoming connection details for the component
endpoints:
  # +required Unique name for the endpoint.
  # This name will be used when generating the managed API
  - name: employeetravelbooking
    # +optional Display name for the endpoint.
    displayName: EmployeeTravelBooking
    # +required Service section has the user service endpoint details
    service:
      # +optional Base path of the API that gets exposed via the endpoint.
      # This is mandatory if the endpoint type is set to REST, GraphQL or WS.
      basePath: /travelmgt/v1
      # +required Numeric port value that gets exposed via the endpoint
      port: 8081
    # +required Type of traffic that the endpoint is accepting.
    # Allowed values: REST, GraphQL, GRPC, TCP, UDP, WS.
    type: REST
    # +optional Network level visibilities of the endpoint.
    # Accepted values: Project|Organization|Public(Default).
    networkVisibilities: 
      - Public
      - Project
    # +optional Path to the schema definition file. Defaults to wild card route if not provided
    # This is only applicable to REST and WS endpoint types.
    # The path should be relative to the docker context.
    schemaFilePath: openapi.json

dependencies:
  serviceReferences:
    - name: choreo:///apifirst/testeuproject/hr-emp-x/ad088/v1/PUBLIC
      connectionConfig: 01efa0ce-8e20-1dc0-b8a2-f4ee4d71d7a7
      env:
      - from: ServiceURL
        to: CHOREO_NEWCONN2_SERVICEURL
      - from: ConsumerKey
        to: CHOREO_NEWCONN2_CONSUMERKEY
      - from: ConsumerSecret
        to: CHOREO_NEWCONN2_CONSUMERSECRET
      - from: TokenURL
        to: CHOREO_NEWCONN2_TOKENURL
  #connectionReferences:
 # - name: NewConn2
  #  resourceRef: service:/testeuproject/hr-emp-x/v1/ad088/PUBLIC
