apiVersion: core.choreo.dev/v1alpha1
kind: ComponentConfig
metadata:
  name: Travel Booking
  projectName: Travel
  annotations:
    componentType: Service
spec:
  # +optional Build instructions for the component.
  build:
    # +required Name of the branch. This is used to identify the corresponding deployment track.
    branch: main
  # +optional Incoming connection details for the component (AKA Endpoints).
  inbound:
    - name: EmployeeTravelBookingAPI
      port: 8081
      type: REST
      networkVisibility: Public
      context: /travelmgt/v1
      schemaFilePath: openapi.json
    - name: EmpBooking1
      port: 8082
      type: REST
      networkVisibility: Project
      context: /test/v1
      schemaFilePath: openapi.json  
  #outbound:
    #serviceReferences:
      #- name: choreo:///apifirst/test514/ufgatw/ad088/v1/PUBLIC
      #  connectionConfig: 01ef1274-77f7-1a78-8496-7ef4c226d5f0
      #  env:
      #  - from: ServiceURL
      #    to: SERVICE_URL
      #  - from: ConsumerKey
      #    to: CLIENT_ID
      #  - from: ConsumerSecret
      #    to: CLIENT_SECRET
      #  - from: TokenURL
      #    to: TOKEN_URL
